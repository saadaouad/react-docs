import React from "react";
import ReactDOM from "react-dom";

it("useDropDown renders without crashing", () => {
  const div = document.createElement("div");
  ReactDOM.render(<useDropDown />, div);
});
